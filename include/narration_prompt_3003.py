# 案内役は名乗り(自己紹介)を最初のシーンで一度だけ行えば十分で、以降のシーンで
# 「◯◯が解説します」のような前置きを毎回書かせると、ナレーションの主役が
# テーマそのものではなく案内役の挙動になってしまう(学園ドラマ化する)不具合が
# 実用系(ITの教室・お金の教室)でも起きていた。名乗りが許されるのはscene_no 1
# を含む最初のチャンクのみとする
GUIDE_NAME_RULE = (
    "案内役は最初に一度だけ名乗って自己紹介してよい"
    if start == 1
    else "案内役の名乗り・呼びかけ・「◯◯が解説します/語ります」のような前置きは一切書かない(名乗りは冒頭のシーンで済んでいる)。ナレーションは解説内容そのものを直接語ることに専念する"
)

narration_prompt = f"""
{BASE}

以下のシーン骨子を、ナレーション本文に詳細化してください。

出力は以下の形式のみ。
JSON禁止。
Markdown禁止。
説明文禁止。

形式：
{start}|ナレーション本文
{start + 1}|ナレーション本文
{start + 2}|ナレーション本文

重要：
絶対に 1 から始めないこと。
今回の開始番号は {start} です。
必ず {start}| から始めて、{end}| で終えること。

設計書：

{design_text}

重要ルール：

ナレーション中の登場人物名は上記の設計書のnameから正確に引用すること。

条件：
- 日本語で書くこと(英語や他の言語の単語・文章を混在させない。登場人物名も英語表記(ローマ字)ではなく、設計書のnameのまま漢字・カタカナ・ひらがなで表記する)
- scene_no {start} から {end} までのみ出力
- 各narrationは100〜160文字
- ナレーションだけで状況が分かるようにする
- {GUIDE_NAME_RULE}
- 主役は案内役ではなく、テーマそのものである。案内役の仕草・表情・相槌などの行動描写は入れない
- 骨子の流れを守る
- 案内役の名前は固定する

シーン骨子：
{json.dumps(chunk_outline, ensure_ascii=False, indent=2)}
"""
